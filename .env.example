# === LLM 設定 ===

# 選項 1: 使用 OpenAI 或 OpenAI 相容 API (地端或雲端)
AZURE_OPENAI_ENDPOINT=http://localhost:8080
AZURE_OPENAI_API_KEY=your-api-key-here
AZURE_OPENAI_DEPLOYMENT=gpt-4o

# 選項 2: 使用 Google Vertex AI
# 如果不設定上述 AZURE_* 變數，系統會自動使用 Vertex AI
# 需要設定 Google Cloud 憑證檔案路徑
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/your-service-account-key.json

# === 應用設定 ===

# Session 資料儲存目錄
SESSION_DIR=session_data

# 日誌層級 (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# 模組日誌層級
MODULE_LOG_LEVEL=INFO

# === API 安全設定 (可選，生產環境建議啟用) ===

# API 金鑰驗證 (在 api_server.py 中需取消註解相關程式碼)
# API_KEY=your-secure-random-api-key-here
